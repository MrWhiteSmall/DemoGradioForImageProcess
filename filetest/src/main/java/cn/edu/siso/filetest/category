三种存储方式
文件存储，Shared Preference存储，SQlite存储

FileOutputStream  openFileOutput(filename,Mode_Private/Mode_Append)
BufferWriter   new BufferWriter(new OutputStreamWrier(fos))
FileInputStream  openFileInput(filename,Mode...)
BufferReader   new BufferReader(new InputStreamReader(fis)

sharedPreference
1,Context 中的  两个参数  name+mode
2，Activity 中的  一个参数  mode  默认当前类名
3，PreferenceManager 中的  一个参数  context  默认当前包名

litepal数据库 使用顺序
build.gradle 导包 implementation 'org.litepal.android:core:1.3.2'
Manifest 添加application信息   org.litepal.LitePalApplication
assets 创建litepal.xml <litepal>
                            <dbname value="">
                            <version value="">
                            <list><mapping class=""></list>
                       </litepal>
创建bean extends DataSupport
数据库创建成功  Connector.getDatabase()
增删改查
Bean bean = new Bean(......) bean.save()
Bean bean = new Bean(..  ..) bean.updateAll("name=? and price=?","name","price")类似....
DataSupport.deleteAll DataSupport.delete(Bean.class,"price<?","price")类似....
DataSupport.findAll(Bean.class)  -->  List<Bean>  查找全部
DataSupport.select().where().limit().offset().order().find(Bean.class)
            findFirst(Bean.class)  findLast(Bean.class)
****Cursor   DataSupport.findBySQL("select * from tablename .....")*****



